## Highlights

- Added a full release history view inside the About dialog instead of only showing a single compare link.
- Each changelog card now links directly to its matching GitHub release page.
- After a successful in-app update and relaunch, the app now opens the changelog automatically and focuses the version that was just installed.
- Hardened local browser-preview behavior so desktop bridge fallbacks no longer break local verification flows.

## UX Changes

- The changelog modal now renders multiple historical releases with publish dates and markdown content.
- The updated-version notice is shown inline above the focused release note after restart.
- Release-note content remains editable from GitHub Releases later without affecting updater metadata or install behavior.

## Verification

- Verified changelog history rendering locally on the app preview.
- Verified post-update changelog auto-open flow locally with a dev-only test hook.
- Verified local Tauri production build after these changes.
